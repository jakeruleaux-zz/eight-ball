{{#if updateAnswerForm}}
<h1>Edit</h1>
  <div>
    <form>
      <div class="form-group">
        <label for="master">Name</label>
        {{input value=master id="master"}}
      </div>
      <div class="form-group">
        <label for="qreply">Reply</label>
        {{input value=qreply id="qreply"}}
      </div>
      <button {{action 'update' answer}}>Save</button>
    </form>
  </div>
{{else}}
  <button {{action 'updateAnswerForm'}}>Edit</button>
{{/if}}
